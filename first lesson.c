// c is a multipurpose prog language
#include<stdio.h> //this includes the header file named stdio that has predefined functions and keywords
int main()  //prog starts from main function , defined already in the header file 
{
    printf("hello world !!"); //printf is already defined to give required text output () ye lagake "" inke andar string likho then ; isse har kisi statement ko terminate karte hain

    return 0;
    // just an indicator to see if the compilation is done succesfully or not 0= success otherwise fail 
}
// main ke baad {} ke andar main code ayega

